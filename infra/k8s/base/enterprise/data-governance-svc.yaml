apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-governance-svc
  labels:
    app: data-governance-svc
    app.kubernetes.io/part-of: aivo-enterprise
spec:
  replicas: 2
  selector:
    matchLabels: { app: data-governance-svc }
  template:
    metadata:
      labels: { app: data-governance-svc }
    spec:
      containers:
        - name: app
          image: aivo/data-governance-svc:latest
          ports: [{ name: http, containerPort: 3070 }]
          env:
            - { name: NODE_ENV, value: production }
            - { name: PORT, value: "3070" }
            - { name: DATA_GOVERNANCE_PORT, value: "3070" }
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef: { name: aivo-common-env, key: DATABASE_URL }
            - { name: AUDIT_SVC_URL, value: "http://audit-svc:3069" }
            - name: AIVO_FEATURE_DATA_GOVERNANCE_CENTER
              valueFrom:
                configMapKeyRef: { name: aivo-enterprise-flags, key: AIVO_FEATURE_DATA_GOVERNANCE_CENTER, optional: true }
          readinessProbe: { httpGet: { path: /healthz, port: http }, initialDelaySeconds: 5 }
          livenessProbe: { httpGet: { path: /healthz, port: http }, initialDelaySeconds: 15 }
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits: { cpu: "1", memory: 1Gi }
---
apiVersion: v1
kind: Service
metadata:
  name: data-governance-svc
spec:
  selector: { app: data-governance-svc }
  ports: [{ name: http, port: 3070, targetPort: http }]
