apiVersion: apps/v1
kind: Deployment
metadata:
  name: recommendation-svc
  labels:
    app: recommendation-svc
    app.kubernetes.io/part-of: aivo-enterprise
spec:
  replicas: 2
  selector:
    matchLabels: { app: recommendation-svc }
  template:
    metadata:
      labels: { app: recommendation-svc }
    spec:
      containers:
        - name: app
          image: aivo/recommendation-svc:latest
          ports: [{ name: http, containerPort: 3066 }]
          env:
            - { name: NODE_ENV, value: production }
            - { name: PORT, value: "3066" }
            - { name: RECOMMENDATION_PORT, value: "3066" }
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef: { name: aivo-common-env, key: DATABASE_URL }
            - { name: AUDIT_SVC_URL, value: "http://audit-svc:3069" }
            - name: AIVO_FEATURE_PROFILE_RECOMMENDATIONS_V2
              valueFrom:
                configMapKeyRef: { name: aivo-enterprise-flags, key: AIVO_FEATURE_PROFILE_RECOMMENDATIONS_V2, optional: true }
            - name: AIVO_FEATURE_DATA_GOVERNANCE_CENTER
              valueFrom:
                configMapKeyRef: { name: aivo-enterprise-flags, key: AIVO_FEATURE_DATA_GOVERNANCE_CENTER, optional: true }
          readinessProbe: { httpGet: { path: /healthz, port: http }, initialDelaySeconds: 5 }
          livenessProbe: { httpGet: { path: /healthz, port: http }, initialDelaySeconds: 15 }
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits: { cpu: "1", memory: 1Gi }
---
apiVersion: v1
kind: Service
metadata:
  name: recommendation-svc
spec:
  selector: { app: recommendation-svc }
  ports: [{ name: http, port: 3066, targetPort: http }]
