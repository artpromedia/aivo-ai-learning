{
  "name": "@aivo/enterprise-core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared enterprise primitives: tenant context, request context, audit context, role policy helpers, and a Fastify auth hook. Used by enterprise services and adapters to enforce consistent governance over learner profile mutations.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": ["src", "dist", "package.json"],
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "test": "vitest run"
  },
  "peerDependencies": {
    "fastify": "^5.0.0"
  },
  "peerDependenciesMeta": {
    "fastify": { "optional": true }
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "fastify": "^5.3.3",
    "typescript": "^5.7.3",
    "vitest": "^2.0.0"
  }
}
