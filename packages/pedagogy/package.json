{
  "name": "@aivo/pedagogy",
  "version": "0.1.0",
  "private": true,
  "description": "Pedagogical strategy primitives — scaffolding, retrieval practice, spaced repetition (SM-2 / Leitner), error correction. Pure functions consumed by tutor-runtime to schedule the next beat for a learner (§9.3 Greenfield 6–8w).",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist", "package.json"],
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.3",
    "vitest": "^2.0.0"
  }
}
