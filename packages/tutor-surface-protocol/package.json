{
  "name": "@aivo/tutor-surface-protocol",
  "version": "0.1.0",
  "private": true,
  "description": "Structured protocol the AI tutor uses to request learner surfaces (scratchpad, geometry, graph, number line, manipulative, annotation, science diagram). Validators reject raw HTML/SVG, profile-incompatible commands, and missing accessibility metadata.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src",
    "dist",
    "package.json"
  ],
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.3",
    "vitest": "^2.0.0"
  }
}
