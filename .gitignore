node_modules/
dist/
dist-test/
.turbo/
*.tsbuildinfo
.env
.env.local
.env.*.local
coverage/
.next/
out/

# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.venv/
.coverage
.pytest_cache/
htmlcov/

# JWT keys (generated by scripts/dev-up.sh)
.keys/
.local/

# Terraform
.terraform/
*.tfstate
*.tfstate.backup

# E2E test artifacts
e2e/reports/
e2e/test-results/
e2e/jwt-private.pem
e2e/jwt-public.pem

# Temporary / scratch files (any extension, also dirs).
# These must never be committed: they routinely contain debug PII,
# operational secrets, build logs, raw network configs, etc.
scripts/diag-*.mjs
tmp_*
tmp_*/

# Replit / Nix workspace placeholder kept in repo root by mistake.
# A real entrypoint lives in apps/marketing (see start.sh).
/main.py

# Secret scan reports
gitleaks-report.json

# Temporary helm artifacts
infra/helm/aivo-ingress.yaml
infra/helm/values/image-overrides.yaml
.data/
