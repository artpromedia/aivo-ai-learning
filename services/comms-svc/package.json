{
  "name": "@aivo/comms-svc",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "pnpm --filter @aivo/db run build && pnpm --filter @aivo/db run db:migrate && node --test --import tsx tests/*.test.ts"
  },
  "dependencies": {
    "@aivo/db": "workspace:*",
    "@aivo/observability": "workspace:*",
    "@aivo/ops-alerts": "workspace:*",
    "@aivo/scheduling": "workspace:*",
    "@aivo/security": "workspace:*",
    "@fastify/cors": "^11.0.0",
    "@fastify/swagger": "^9.6.0",
    "@fastify/swagger-ui": "^5.2.2",
    "drizzle-orm": "^0.44.2",
    "fastify": "^5.3.3",
    "jose": "^6.0.11",
    "postmark": "^4.0.7"
  },
  "devDependencies": {
    "tsx": "^4.19.4",
    "typescript": "^5.7.3"
  }
}
