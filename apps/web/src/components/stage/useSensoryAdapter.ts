// Re-export shim — canonical source moved to `@aivo/stage-runtime` as part
// of the §8 #2 Stage extraction (v2.1).
export { useSensoryAdapter } from "@aivo/stage-runtime";
export type { SensoryAdapterApi } from "@aivo/stage-runtime";
