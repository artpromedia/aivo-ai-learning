// Re-export shim — canonical source moved to `@aivo/stage-runtime` as part
// of the §8 #2 Stage extraction (v2.1).
export { matchVoiceAnswer, normalizeAnswer } from "@aivo/stage-runtime";
export type { MatchResult } from "@aivo/stage-runtime";
